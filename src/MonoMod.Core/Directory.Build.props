<Project>
  
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <Import Project="$(MSBuildThisFileDirectory)/Platforms/Architectures/NativeHelpers.props" />

</Project>
